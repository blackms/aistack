apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aistack.fullname" . }}-config
  labels:
    {{- include "aistack.labels" . | nindent 4 }}
data:
  agentstack.config.json: |
    {{- toJson .Values.config.contents | nindent 4 }}
